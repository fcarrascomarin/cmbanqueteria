
const money=v=>new Intl.NumberFormat('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0}).format(v||0);
async function loadTodayMenu(){const box=document.querySelector('#todayMenu');try{const res=await fetch('/api/public/menu/today'),data=await res.json();if(!data.menu){box.innerHTML='<h3>Consulta el menú disponible</h3><p class="muted">Hoy aún no se ha publicado un menú en la web. Puedes consultar directamente con CM Banquetería.</p>';return}const m=data.menu;box.innerHTML=`<span class="badge green">Publicado para hoy</span><h3 style="margin-top:10px">${m.title}</h3><p class="muted">Plato principal: ${m.main_dish||'No informado'}<br>Acompañamiento: ${m.side_dish||'No informado'}<br>Ensalada: ${m.salad||'No informada'}<br>Postre: ${m.dessert||'No informado'}</p><strong style="font-size:1.4rem;color:var(--vino)">${money(m.price)}</strong><p class="muted">Raciones disponibles: ${m.available_portions||0}</p>`}catch(e){box.innerHTML='<p class="muted">No se pudo cargar el menú del día.</p>'}}
document.querySelector('#quoteForm')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,payload=Object.fromEntries(new FormData(form).entries()),btn=form.querySelector('button');try{btn.disabled=true;btn.textContent='Enviando...';const res=await fetch('/api/public/quotes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await res.json();if(!res.ok)throw Error(data.error||'No se pudo enviar la cotización.');alert('Solicitud enviada correctamente. CM Banquetería se contactará contigo.');form.reset()}catch(err){alert(err.message)}finally{btn.disabled=false;btn.textContent='Enviar solicitud'}});loadTodayMenu();
